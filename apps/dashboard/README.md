# Public Opinin Dashboard

Next.js dashboard for filtering and reviewing posts collected by the aggregator.

## Setup

```bash
npm install
cp .env.example .env.local
npm run dev
```

The dashboard expects the aggregator API at `NEXT_PUBLIC_AGGREGATOR_API_URL`.
